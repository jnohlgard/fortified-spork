#!/usr/bin/sh
set -eu

printf 'Selecting system images to include on the install disk\n'
mkdir -p /images/
cp -v --reflink=auto --sparse=auto --archive "/mkosi.output/spork_${IMAGE_VERSION}."* /images/
